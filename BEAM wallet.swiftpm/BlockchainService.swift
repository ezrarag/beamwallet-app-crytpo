import Foundation

// MARK: - BlockchainService (deprecated)
// This file is no longer in active use.
// Balance fetching and transaction submission are handled by WalletManager
// via the /api/balance and /api/allocate endpoints at AppConfig.apiBaseURL.
